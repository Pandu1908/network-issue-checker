# network-issue-checker
A Python program that checks network connectivity by taking a website as input and using the os.system() function to execute a ping command. It evaluates the returned status code with an if-else statement to determine whether the network is working properly or a network issue is detected.
